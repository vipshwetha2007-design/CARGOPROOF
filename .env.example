# ---------------- CargoProof ----------------
PORT=4021
DEMO_MODE=true

# x402 / Algorand
FACILITATOR_URL=https://facilitator.goplausible.xyz
PAY_TO=
AVM_MNEMONIC=
VERIFIER_ADDRESS=
ESCROW_APP_ID=
USDC_ASSET_ID=10458941

# Optional: LLM provider. Demo agent works without it.
LLM_API_KEY=
LLM_BASE_URL=
LLM_MODEL=

# CargoProof Assistant (chatbot) — LLM providers
# Choose which provider the assistant uses. If the selected provider's
# key is missing or a call fails, the assistant falls back to the other
# provider (if configured), and finally to a deterministic templated
# response so it never goes silent.
AI_PROVIDER=gemini
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash
GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile

# Algorand
ALGOD_URL=https://testnet-api.algonode.cloud
INDEXER_URL=https://testnet-idx.algonode.cloud
